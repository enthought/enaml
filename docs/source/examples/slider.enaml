defn MainWindow:
    Window:
        title = "Slider example"
        constraints = [vertical(top, _space_, slider, _space_, bottom),
                        horizontal(left, slider, right),
                        slider.width >= 100]
        Slider -> slider:
            value = 30
            maximum = 100
            tick_position = 'top'


defn MainWindow:
    Window:
        title = "Slider example"
        constraints = [vertical(top, spacer, slider, spacer, bottom),
                        horizontal(left, slider, right),
                        slider.width >= 100]
        Slider -> slider:
            value = 30
            maximum = 100
            tick_position = 'top'




queue ->    
    implicit via zmq
    or explicit via actual list
    

sampling -> 
    

subscription -> 


manager 
    source -> renderers


manager:
    datasource_id -> datasources
    datasource_id -> [renderer_id]
    renderer_id -> renderbuffer


while wakeup():
    for notification in notifications:
        get_datasource(notification)
        get_subscribers(datasource)
        update_subscriber_renderbuffers


Datasource():
    while(wake_up):
        queue
    

while wake_up():
    filter new samples
    